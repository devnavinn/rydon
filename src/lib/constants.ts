/**
 * Time zone for dates rendered on the server (public ride pages, share
 * images), where there's no viewer locale to fall back on. Rydo's riders
 * are India-based (Mappls maps), so ride times are shown in IST.
 */
export const DISPLAY_TIME_ZONE = "Asia/Kolkata";
